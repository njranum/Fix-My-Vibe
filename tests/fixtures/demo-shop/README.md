# demo-shop

A small storefront API for browsing products and taking orders, built with Flask
and SQLite. Vibe-coded quickly — used as a Fix My Vibe demo fixture with a handful
of fixable problems: no AI tool config, leaked secrets, and a couple of security
patterns AI assistants commonly introduce.

```bash
pip install -r requirements.txt
flask --app app run
```
