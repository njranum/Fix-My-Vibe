[project]
name = "order-tracker"
version = "0.1.0"
description = "FastAPI service for tracking customer orders"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
