.env
.env.*
!.env.example
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.DS_Store
*.bak
!tests/fixtures/**/.env

# Fix My Vibe timing traces (--trace / FMV_TRACE)
.fmv-traces/

# Fix My Vibe throwaway working copies (run harness + /fix-my-vibe command)
.fmv-run/

# Hero demo video — hosted via GitHub's asset CDN (too big to preview/embed as a repo file)
docs/media/*.mp4
